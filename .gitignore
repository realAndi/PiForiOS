# Built artifacts -- regenerated by tools/ and by the GitHub Action.
packaging/payload/libshim.dylib
repo/debs/*.deb
repo/Packages
repo/Packages.*
repo/Release
repo/index.html
repo/depiction.html
*.bak
.DS_Store

# Cloned by tools/ci.sh; build artifact written by tools/build-payload.sh.
.ci/
packaging/payload/PAYLOAD.version

# py_compile leaves these beside the scripts; publish.yml narrows payload-paths
# for the same reason, but they should never reach a commit either.
__pycache__/
